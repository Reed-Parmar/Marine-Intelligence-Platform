# Machine Learning (ML)

This directory will host machine learning models, training pipelines, inference modules, and model artifacts.

## Future Responsibilities
- Species classification and identification models
- Otolith shape and morphometric feature extraction (CNN / ONNX inference)
- Model weights, ONNX export artifacts, and versioning utilities
- Inference endpoints and integration with backend services
